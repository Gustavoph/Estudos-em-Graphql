type Mutation{
    # Mutation de usuario
    novoUsuario(
        dados: UsuarioInput!
    ): Usuario!

    excluirUsuario(
        filtro: UsuarioFiltro!, 
        ): Usuario

    alterarUsuario(
        filtro: UsuarioFiltro!,
        dados: UsuarioInput!
    ) : Usuario
}
