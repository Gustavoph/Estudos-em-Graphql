const Query = require('./Query');
const Usuario = require('./Usuario');
const Mutation = require('./Mutation');
const PerfisMutation = require('./PerfisMutation');

module.exports = {
    Query,
    Usuario,
    Mutation,
    PerfisMutation
}